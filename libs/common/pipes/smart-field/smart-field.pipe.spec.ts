import { SmartFieldPipe } from './smart-field.pipe';

describe('SmartFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new SmartFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
